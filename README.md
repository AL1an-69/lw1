# Сравнение методов вычисления расстояния Левенштейна

## Что тут есть
Реализовал алгоритм Левенштейна на чистом Python и сравнил его с библиотеками `python-Levenshtein` и `fuzzywuzzy`. Также добавил модуль для чтения текста из `.docx` файлов.

## Как запустить (через uv)
1. Добавь зависимости (они сами пропишутся в `pyproject.toml`):
```bash
uv add python-docx fuzzywuzzy python-Levenshtein